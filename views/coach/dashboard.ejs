<%- include('../partials/header', { title: 'Dozent/in-Dashboard - DABuddy', user: user }) %>

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Meine betreuten Diplomarbeiten</h1>
    <div class="d-flex align-items-center gap-3">
        <span class="text-muted"><i class="bi bi-calendar"></i> Diplomjahr: <%= selectedYear %></span>
        <%- include('../partials/role-switcher', { availableRoles: availableRoles, currentRole: currentRole }) %>
    </div>
</div>

<% if (theses.length === 0) { %>
    <div class="alert alert-info" role="alert">
        <i class="bi bi-info-circle"></i>
        Im ausgewählten Diplomjahr sind Ihnen keine Diplomarbeiten zur Betreuung zugewiesen.
    </div>
<% } else { %>
    <div class="row">
        <% theses.forEach(function(thesis) { %>
            <div class="col-md-6 mb-4">
                <div class="card h-100">
                    <div class="card-header">
                        <h6 class="mb-0"><i class="bi bi-file-text"></i> <%= thesis.title %></h6>
                    </div>
                    <div class="card-body">
                        <p><strong>Fachbereich:</strong> <%= thesis.department.name %></p>
                        <% if (thesis.sponsor) { %>
                            <p><strong>Auftraggeber:</strong> <%= thesis.sponsor %></p>
                        <% } %>
                        <div class="mt-3">
                            <h6>Studenten:</h6>
                            <% thesis.students.forEach(function(s) { %>
                                <div class="mb-2">
                                    <i class="bi bi-person-fill"></i> <%= s.name %>, <%= s.firstname %>
                                    <br><small class="text-muted"><%= s.email %></small>
                                </div>
                            <% }); %>
                        </div>
                        <% if (thesis.experts.length > 0) { %>
                            <div class="mt-3">
                                <h6>ExpertInnen:</h6>
                                <% thesis.experts.forEach(function(e) { %>
                                    <div class="mb-2">
                                        <i class="bi bi-award"></i> <%= e.name %>, <%= e.firstname %>
                                        <br><small class="text-muted"><%= e.email %></small>
                                    </div>
                                <% }); %>
                            </div>
                        <% } %>
                    </div>
                    <div class="card-footer">
                        <a href="/dashboard/thesis/<%= thesis.id %>" class="btn btn-primary btn-sm">
                            <i class="bi bi-eye"></i> Details & Meilensteine
                        </a>
                    </div>
                </div>
            </div>
        <% }); %>
    </div>
<% } %>

<%- include('../partials/footer') %>
